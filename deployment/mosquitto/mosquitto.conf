# Config minimale, LAN-only, sans authentification (cohérent avec le "TODO Keycloak"
# déjà noté ailleurs dans le projet : rien de tout ceci n'est exposable sur internet en l'état).
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
