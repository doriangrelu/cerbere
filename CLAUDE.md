# CLAUDE.md — Cerbère

Ce fichier est maintenu par l'assistant IA au fil des itérations. Voir la section [Règles de mise à jour](#règles-de-mise-à-jour-pour-lia) en bas de fichier.

## Vue d'ensemble

Cerbère est un système d'alarme domestique développé de zéro (usage personnel, une seule maison), **open source**. Backend microservices Java/Spring Boot, MongoDB, Kafka. Deux interfaces prévues à terme (réseau interne / internet), non développées pour l'instant — voir [docs/prd/000-vision-produit.md](docs/prd/000-vision-produit.md). Vue d'entrée du projet : [README.md](README.md).

Itération en cours : **`cerbere-devices-bridge`** (supervision du matériel physique via MQTT/Zigbee2MQTT — contact, mouvement, sirène) + containerisation Docker Compose + doc de déploiement NUC. Détail : [docs/prd/001-mvp-backend-alarme.md](docs/prd/001-mvp-backend-alarme.md).

## Architecture

Vue complète et schéma : [docs/architecture/vue-ensemble.md](docs/architecture/vue-ensemble.md).

Résumé : `api-gateway` (routage) → `cerbere-bff` (agrégation REST) → `cerbere-core`/`cerbere-history` (REST). Communication événementielle via Kafka entre `cerbere-devices-mock`/`cerbere-devices-bridge` → `cerbere-core`/`cerbere-history`, et `cerbere-core` → `cerbere-history`/`cerbere-notification`/`cerbere-devices-bridge`. `cerbere-devices-bridge` dialogue en plus avec le matériel Zigbee réel via MQTT (passerelle Zigbee2MQTT) — voir [docs/architecture/mqtt-zigbee-contracts.md](docs/architecture/mqtt-zigbee-contracts.md).

## Stack technique

- Java 25, Spring Boot 4.1.0 (`spring-boot-starter-parent` en parent de chaque module).
- MongoDB (souplesse de schéma) — une base logique par service, voir [ADR 0003](docs/adr/0003-mongodb-database-per-service.md).
- Kafka (message broker inter-microservices) — conventions dans [docs/best-practices/kafka-conventions.md](docs/best-practices/kafka-conventions.md).
- Spring Cloud Gateway (MVC/servlet, pas WebFlux) pour `api-gateway`.
- Lombok, Spring DevTools, Spring Boot Docker Compose support.
- Pas de pom agrégateur héritant — chaque module a son propre `spring-boot-starter-parent`. Un pom racine `pom` (reactor uniquement, sans héritage) existe pour builder tous les modules ensemble — voir [ADR 0009](docs/adr/0009-pom-agregateur-racine-optionnel.md).
- Sécurité (Keycloak) : dépendances déjà présentes dans les poms, non activées. Voir [ADR 0007](docs/adr/0007-authentification-keycloak-phase-2.md).
- Publication : chaque module publie son package sur GitHub Packages (`distributionManagement` dans chaque pom, workflow `.github/workflows/publish-packages.yml`) — voir [ADR 0011](docs/adr/0011-publication-github-packages.md).
- Front interne (`cerbere-bff`) : Thymeleaf server-side + [htmx](https://htmx.org/)(-spring-boot) + [Beer CSS](https://www.beercss.com/) (Material Design 3, sans build JS) — voir [ADR 0015](docs/adr/0015-stack-front-interne-thymeleaf-htmx-beercss.md) et [docs/best-practices/frontend-conventions.md](docs/best-practices/frontend-conventions.md). `htmx-spring-boot` n'est **pas** utilisée (incompatible Boot 4.1 pour l'instant) : htmx brut en CDN.

## Conventions de code

Détail complet : [docs/best-practices/coding-standards.md](docs/best-practices/coding-standards.md) et [docs/best-practices/clean-architecture.md](docs/best-practices/clean-architecture.md).

Résumé : clean architecture stricte par module (`domain`/`application`/`infrastructure`/`adapter`), sous-packagée par agrégat dès qu'un `port/in`/`port/out` dépasse 3-4 interfaces (ex : `port/in/device`, `port/in/zone`, `port/in/alarm`), SOLID, `final` systématique (paramètres, variables locales, champs — y compris `this.` obligatoire sur tout accès à un champ/méthode d'instance), records pour les value objects/événements/DTOs, suffixe systématique par rôle (`UseCase`/`Service`/`Repository`/`Document`/`Envelope`/... — voir tableau dans coding-standards.md), méthodes référence plutôt que lambdas triviales, JavaDoc obligatoire sur `domain`, pas de commentaires qui répètent le code. Lombok (`@RequiredArgsConstructor`, `@Getter`) pour le boilerplate DI/accesseurs, MapStruct (sans expression Java, méthodes nommées/communes via `uses=`) pour les traducteurs structurels entre couches, normalisation trim+lowercase des champs texte libre en couche présentation — voir coding-standards.md. Le module `cerbere-shared-kernel` (package `fr.cerbere.shared`) porte les objets et configurations communs (enveloppe Kafka, sérialiseur JSON, mapper/sécurité par défaut) — voir [ADR 0013](docs/adr/0013-module-cerbere-shared-kernel.md).

## Structure du repo

| Module | Rôle | Statut |
|---|---|---|
| `api-gateway` | Routage/sécurité (Spring Cloud Gateway) | Scaffold vide |
| `cerbere-bff` | Front interne (Thymeleaf/htmx/Beer CSS) + agrégation REST | En cours (alarme, devices, zones, mode test, historique faits) |
| `cerbere-core` | Domaine alarme : zones, devices, arm/disarm, alertes | **Implémenté** |
| `cerbere-history` | Historique des événements/alertes | **Implémenté** |
| `cerbere-notification` | Envoi d'alertes (email/push) | Scaffold vide |
| `cerbere-devices-mock` | Simulation de devices (contact, mouvement, sirène) | **Implémenté** |
| `cerbere-devices-bridge` | Supervision du matériel physique via MQTT/Zigbee2MQTT (contact, mouvement, sirène) | **Implémenté** |
| `cerbere-shared-kernel` | Objets/configs communs (`EventEnvelope`, `JacksonEventSerializer`, `CommonJacksonConfig`, `PermitAllSecurityConfig`) | **Implémenté** |
| `deployment` | `docker-compose.yaml` (Mongo, Kafka, `cerbere-core`/`bff`/`history` toujours démarrés, profils `mock`/`hardware`) | Containerisé (voir [docs/deployment/nuc-deployment.md](docs/deployment/nuc-deployment.md)) |
| `docs` | PRD, ADR, bonnes pratiques, architecture, déploiement | À jour |
| `.github/workflows` | CI de publication GitHub Packages | `publish-packages.yml` (6 modules) |
| `README.md` | Vue d'entrée du projet (open source) | À jour |

## Lancer en local

**Hors conteneur** (dev, hot-reload) :

```bash
# Infra (Mongo + Kafka)
cd deployment && docker compose up -d mongodb kafka

# cerbere-shared-kernel doit être installé en local avant tout module qui en dépend
# (pas de pom agrégateur racine, voir ADR 0009/0013)
cd cerbere-shared-kernel && ./mvnw install -DskipTests

# cerbere-devices-mock
cd cerbere-devices-mock && ./mvnw spring-boot:run
```

**Conteneurisé** (démo ou matériel réel), voir [docs/deployment/nuc-deployment.md](docs/deployment/nuc-deployment.md) pour le détail :

```bash
cd deployment
docker compose --profile mock up -d       # démo/dev sans matériel
docker compose --profile hardware up -d   # matériel Zigbee réel
```

`api-gateway`/`cerbere-notification` restent des scaffolds vides, ni lancés ni containerisés.

## Journal des itérations

| Date | Itération | Livré | Références |
|---|---|---|---|
| 2026-07-22 | Fondations + devices-mock | Structure `docs/` complète (PRD, 9 ADR, best-practices, vue d'architecture), `CLAUDE.md` racine, module `cerbere-devices-mock` complet (clean architecture, simulation contact/mouvement/sirène, producteur Kafka, scheduler + API de contrôle), correction `deployment/docker-compose.yaml` (renommage + config Kafka KRaft) | [PRD 001](docs/prd/001-mvp-backend-alarme.md), ADR 0001-0009 |
| 2026-07-22 | Règles additionnelles | Module `contract` partagé autorisé (ADR 0010, supersede ADR 0005), suffixe systématique par rôle + `this.` obligatoire (coding-standards.md), renommage `PublishDeviceEventUseCase` → `PublishDeviceEventService`, config GitHub Packages sur les 6 modules + workflow CI (ADR 0011), `README.md` racine, correction du conflit de bean `ObjectMapper`/`JsonMapper` (`@Primary` retiré de `MapperConfig`), sérialiseur Kafka Jackson 3 écrit à la main (`DeviceEventEnvelopeSerializer`, ADR 0012) suite à l'incompatibilité du `JsonSerializer` Jackson 2 de spring-kafka. **Vérifié bout-en-bout** : device créé, événement déclenché, message confirmé sur `cerbere.device.events.raw` via `kafka-console-consumer` | ADR 0010, ADR 0011, ADR 0012 |
| 2026-07-22 | Module `cerbere-shared-kernel` | Module partagé créé (`fr.cerbere.shared`) : `EventEnvelope`/`JacksonEventSerializer` (généralisés depuis les équivalents `Device*` de `cerbere-devices-mock`), `CommonJacksonConfig`/`PermitAllSecurityConfig` (configs communes). `cerbere-devices-mock` migré pour en dépendre, fichiers dupliqués supprimés. Workflow CI corrigé : `chmod +x mvnw` (fix `Permission denied`) + job dédié publiant `cerbere-shared-kernel` avant les autres modules (`needs:`) | [ADR 0013](docs/adr/0013-module-cerbere-shared-kernel.md) |
| 2026-07-22 | `cerbere-core` complet | Domaine (Device/Zone/AlarmSystem avec `ArmingMode` AWAY/HOME sans état DISARMED représentable, CRUD complet device/zone), ports sous-packagés par agrégat, use-cases, persistence Mongo, producteurs/consommateur Kafka (`cerbere.alarm.state-changed`/`cerbere.alarm.alerts` provisionnés, `cerbere.device.events.raw` consommé), API REST (devices/zones/alarm). Ajout Lombok (`@RequiredArgsConstructor`/`@Getter`) et MapStruct (mappers structurels, méthodes nommées communes `CommonIdMapper`, pas d'expression Java) sur tout le module, normalisation trim+lowercase des champs texte libre en couche présentation (`CommonTextMapper`). Suppression du `compose.yaml` Initializr + dépendance `spring-boot-docker-compose` (redondant avec `deployment/docker-compose.yaml`, faisait planter le démarrage une fois le fichier supprimé). **Vérifié bout-en-bout** : zone/device créés avec normalisation confirmée, armement AWAY publié sur `cerbere.alarm.state-changed` | ADR 0014 |
| 2026-07-22 | Corrélation d'id device | `RegisterDeviceUseCase.register()` prend désormais l'id en entrée (celui du device réel/simulé côté bridge) au lieu d'en générer un, pour que `HandleDeviceEventService` puisse retrouver le device correspondant à un événement Kafka — conforme à l'intention déjà actée en ADR 0004. `Device.register`, le port, le service applicatif, `RegisterDeviceRequest` et `DeviceController` mis à jour en conséquence | ADR 0004 |
| 2026-07-22 | Front interne : tableau de bord alarme | Stack tranchée après recherche (ADR 0015) : Thymeleaf natif (fragments paramétrés, pas `thymeleaf-layout-dialect`) + htmx + Beer CSS. Scaffold `cerbere-bff` (Thymeleaf, RestClient vers `cerbere-core`, configs partagées importées), layout + nav, premier écran complet (statut alarme + armer AWAY/HOME + désarmer, swap htmx sans rechargement). DTOs `AlarmStatusResponse`/`ArmRequest` déplacés dans `cerbere-shared-kernel` (`fr.cerbere.shared.dto.alarm`) après duplication repérée entre `cerbere-core` et `cerbere-bff` — règle rappelée : tout DTO consommé par 2+ modules vit dans le shared-kernel. `htmx-spring-boot`/`htmx-spring-boot-thymeleaf` retirées : incompatibles Spring Boot 4.1 (`WebMvcRegistrations` déplacé), htmx utilisé en CDN brut à la place. **Vérifié dans le navigateur** : layout/nav/Beer CSS rendus, les 3 actions (armer absence/présence, désarmer) mettent à jour le badge de statut sans rechargement complet | ADR 0015 |
| 2026-07-22 | Front interne : administration devices/zones + mode test | DTOs Device/Zone/SimulatedDevice/DeviceEvent déplacés dans `cerbere-shared-kernel` (`fr.cerbere.shared.dto.device`/`zone`/`devicemock`) avant d'ajouter les nouveaux écrans, pour ne pas dupliquer les contrats REST (même règle que pour l'alarme). `cerbere-bff` : clients REST `DeviceCoreClient`/`ZoneCoreClient` (vers `cerbere-core`) et `DeviceMockClient` (vers `cerbere-devices-mock`, second `RestClient` bean sur `cerbere.devices-mock.base-url`) ; écran **Devices** (liste, création avec id/type/label/zone, activer/désactiver, suppression) ; écran **Zones** (liste, création, renommage inline, suppression — le conflit 409 d'une zone non vide est affiché dans le fragment de table plutôt que de planter l'écran) ; écran **Mode test** (`TestModeController`, `@ConditionalOnProperty(cerbere.bff.test-mode.enabled=true)`) pour créer des devices simulés et déclencher manuellement un état (OPEN/CLOSED, DETECTED/CLEAR, ACTIVE/INACTIVE selon le type) sur `cerbere-devices-mock` ; menu de navigation complet, l'entrée "Mode test" étant conditionnée par le même flag exposé globalement aux vues via `NavigationModelAdvice` (`@ControllerAdvice`). **Vérifié bout-en-bout dans le navigateur** (les trois services démarrés ensemble) : CRUD device et zone fonctionnels via htmx (rename, toggle, delete), suppression d'une zone vide acceptée, création de device simulé + déclenchement d'événement fonctionnels, et confirmation que la route `/test-mode` disparaît réellement (404, pas juste le lien de menu) quand `cerbere.bff.test-mode.enabled=false`. Piège relevé et documenté (voir frontend-conventions.md) : `hx-vals` généré dynamiquement doit produire du JSON valide (guillemets doubles), un littéral Thymeleaf avec guillemets simples est silencieusement ignoré par htmx. | ADR 0010/0013 |
| 2026-07-22 | Front interne : correction layout nav + suppression des UUID côté usager | Deux retours utilisateur après la démo. (1) `fragments/nav.html` avait un `<nav>` externe superflu autour du `<header class="fixed primary">`, faisant flotter la barre de navigation à côté du contenu au lieu d'un bandeau fixe en haut — corrigé en faisant du `<header>` la racine du fragment (voir frontend-conventions.md). (2) L'usager ne doit jamais saisir ni lire un UUID : `DeviceAdminController` agrège désormais `cerbere-core` (devices déjà enregistrés) et `cerbere-devices-mock` (devices simulés) pour construire une liste déroulante des devices *disponibles* (non encore enregistrés, affichés par libellé + type) remplaçant le champ id texte libre ; `DeviceRow`/`SimulatedDeviceRow` (nouveaux modèles de présentation locaux au BFF, package `adapter.in.web.<agrégat>`, pas dans le shared-kernel car ce ne sont pas des contrats REST) résolvent `zoneId` en nom de zone avant transmission au template, sur les écrans Devices et Mode test ; écran Zones : colonne Id retirée du tableau (le nom suffit, l'id ne reste que dans les attributs htmx cachés). Tout id soumis par un formulaire (`candidateId` pour un device, `zoneId` pour device/simulated-device) est revérifié côté contrôleur BFF contre la liste courante avant transmission au service métier ; un id ne correspondant à rien est rejeté avec un message affiché dans le fragment plutôt que transmis tel quel. Règle générale "pas d'UUID visible, résolution des clés étrangères en contrôleur, revalidation des ids soumis" ajoutée à frontend-conventions.md. |  |
| 2026-07-22 | Violation persistée + inversion de dépendance mock/core (événementielle) | Deux retours utilisateur supplémentaires, traités ensemble par l'utilisateur puis complétés par l'IA. (1) `Device` porte désormais un flag `violation` persisté (`withViolation`/`withoutViolation`) : `HandleDeviceEventService` le met à jour à chaque événement reçu, et `AlarmSystemService.arm()` vérifie l'ensemble des devices au moment de l'armement — armer alors qu'un capteur est déjà en violation (ex : porte restée ouverte) déclenche l'alarme immédiatement, sans attendre un nouvel événement. **Vérifié dans le navigateur**. (2) Inversion du sens de dépendance actée en ADR 0016 : `cerbere-core` publie `device.created`/`device.updated`/`device.deleted` sur le nouveau topic `cerbere.device.state` (port `DevicePublisher`, adapter `DeviceKafkaProducer`) ; `cerbere-devices-mock` les consomme (`DeviceEventKafkaConsumer`) pour maintenir son miroir `SimulatedDevice` sans jamais interroger le core directement (mécanisme événementiel retenu après une première tentative de client REST mock→core, retirée). L'IA a complété la synchronisation update/delete (côté utilisateur, seul `device.created` était branché) et ajouté la résilience demandée : `HandleDeviceEventService` vérifie l'existence du device *avant* tout traitement (l'ancien ordre provoquait un NPE, reproduit en direct, sur un événement pour un device supprimé du core mais encore connu du mock) ; `DeviceEventKafkaConsumer` (mock) encapsule le traitement dans un try/catch résilient (type d'événement inconnu ou payload incomplet = log + ignore, pas d'exception qui remonte) ; `UpdateSimulatedDeviceService`/`DeleteSimulatedDeviceService` tolèrent un miroir local absent. Nettoyage du code mort laissé par la tentative REST abandonnée (`CoreDeviceLookupPort`/`CoreDeviceLookupClient`/`RestClientConfig` côté mock, `cerbere.core.base-url`, `DeviceMockClient.register()`/`RegisterSimulatedDeviceRequest` côté BFF/shared-kernel, `availableDevices` mort dans `DeviceAdminController`). **Vérifié bout-en-bout** : création d'un device dans le core → apparition automatique dans le mode test ; événement déclenché sur un device supprimé du core → ignoré proprement (plus de crash), les événements suivants sur des devices valides continuent de fonctionner. | [ADR 0016](docs/adr/0016-inversion-dependance-mock-vers-core.md) |
| 2026-07-23 | ProblemDetail partout + unicité label device + rename BFF + pages d'erreur + polish CSS | Retours utilisateur groupés, traités en parallèle. **Gestion des erreurs** : `fr.cerbere.shared.web.CommonExceptionHandler` (nouveau, shared-kernel, importé via `@Import` sur chaque `*Application`) gère les erreurs communes (`MethodArgumentNotValidException`, `HttpMessageNotReadableException`, `IllegalArgumentException`) en `ProblemDetail` (RFC 9457) ; `CoreExceptionHandler`/`DevicesMockExceptionHandler` (locaux à chaque service) réécrits pour renvoyer `ProblemDetail` au lieu de `ResponseEntity<String>`. **Unicité du label device** : `DeviceRepository.findByLabel` + vérification dans `RegisterDeviceService`/`UpdateDeviceService` (exclusion de l'id courant pour l'update), `DuplicateDeviceLabelException` → 409. **BFF** : nouveau `ProblemDetailMessages` (extrait le champ `detail` du JSON `ProblemDetail` plutôt que d'afficher le brut) utilisé dans `DeviceAdminController`/`ZoneAdminController` ; ajout du renommage inline d'un device (`PUT /devices/{id}`, même pattern que Zones) ; pages d'erreur personnalisées (`templates/error/404.html`, `error/5xx.html`, `error.html`) remplaçant la Whitelabel Error Page (`server.error.include-message=always`) ; polish CSS (tables `striped`, espacement entre champs de formulaire via `div.space`, groupement des boutons d'action en `.row`, attributs `required`/`maxlength` cohérents). **Non vérifié dans le navigateur** (l'utilisateur lance et teste lui-même les applications) — compilation et tests unitaires des 4 modules passent. | — |
| 2026-07-23 | Verrouillage optimiste Mongo + badges statut/violation + inputs Material | Trois retours utilisateur traités en parallèle. **`@Version`** sur les 4 agrégats persistés (`DeviceDocument`/`ZoneDocument`/`AlarmSystemDocument` dans `cerbere-core`, `SimulatedDeviceDocument` dans `cerbere-devices-mock`) pour se prémunir des écritures concurrentes (verrouillage optimiste Mongo). Le champ `version` (`Long`, `null` tant que l'entité n'est pas persistée) a dû être ajouté aux modèles de domaine correspondants (`Device`/`Zone`/`AlarmSystem`/`SimulatedDevice`) et propagé par toutes leurs méthodes `with*`/transitions : sans ça, `@Version` change la détection "entité neuve" de Spring Data (basée sur `version == null` plutôt que sur l'id), et toute mise à jour aurait été traitée comme une insertion en doublon. Les mappers (`toDomain`/`toDocument`) portent désormais ce champ ; `register()`/`initial()` restent inchangés (version toujours `null` à la création), seules les fabriques `restore(...)` gagnent un paramètre `version` (aucun test ne les appelait directement). **BFF** : `DeviceResponse` (shared-kernel) expose désormais `violation` (état dérivé `Device.isViolation()`, jamais le drapeau brut) ; écran Devices : badges colorés (`span.chip`) pour le statut (Actif/Inactif) et la violation (Violation en rouge/`error`, OK en neutre), et les champs de renommage inline (Devices et Zones) utilisent désormais le même habillage Material (`div.field.label.border small round`) que les formulaires de création plutôt qu'un `<input>` nu. **Non vérifié dans le navigateur** (l'utilisateur lance et teste lui-même) — compilation et tests unitaires des 4 modules passent. | — |
| 2026-07-23 | Corrections rapides : icône toggle, toasts, unicité zone, majuscules | Quatre petits bugs remontés après test utilisateur. (1) Icône du bouton toggle Devices inversée (`toggle_off` affiché quand `enabled=true`) — corrigée. (2) Toasts (snackbar Beer CSS) sur chaque requête htmx : script générique dans `layout/main.html` écoutant `htmx:afterRequest`, message générique selon le verbe HTTP en succès (Créé/Modifié/Supprimé), champ `detail` du `ProblemDetail` extrait côté client en échec. (3) Unicité du nom de zone, même pattern que le label device (`ZoneRepository.findByLabel` → `findByName`, vérification dans `RegisterZoneService`/`UpdateZoneService` avec exclusion de l'id courant à la mise à jour, `DuplicateZoneNameException` → 409). (4) Affichage en majuscules (`class="upper"`, `text-transform` Beer CSS) de tous les champs libres affichés/édités dans le BFF (labels device, noms de zone, dans les tableaux comme dans les formulaires de création) — la valeur stockée reste inchangée, seul l'affichage est transformé. **Non vérifié dans le navigateur** (l'utilisateur teste lui-même) — compilation et tests unitaires passent. | — |
| 2026-07-23 | ControllerAdvice BFF pour les appels sortants (htmx-aware) | Demande de fermer la dernière fuite d'info : `AlarmDashboardController`/`TestModeController` (entre autres) ne catchaient aucune erreur de leurs appels vers `cerbere-core`/`cerbere-devices-mock`, laissant fuiter un message technique brut via la page d'erreur générique. Premier essai en `@RestControllerAdvice` corrigé immédiatement après retour utilisateur : ce type d'advice force du JSON pour **toute** requête, y compris les navigations de page complète (`/devices`, `/zones`, ...), cassant l'affichage de la page d'erreur stylée pour celles-ci. `BffExceptionHandler` final : simple `@ControllerAdvice`, chaque `@ExceptionHandler` (`HttpStatusCodeException`/`ResourceAccessException`/`RestClientException`) inspecte l'en-tête `HX-Request` — requête htmx → `ProblemDetail` JSON (consommé par le toast) ; navigation classique → vue `error` (page stylée, `${status}`/`${message}` en modèle). Pas de handler générique `Exception.class` (risque d'interférer avec la résolution 404). `CommonExceptionHandler` (shared-kernel) importé sur `CerbereBffApplication` par cohérence, même si peu sollicité côté BFF (pas de `@Valid @RequestBody`). **Non vérifié dans le navigateur** (l'utilisateur teste lui-même) — compilation et tests unitaires passent. | — |
| 2026-07-23 | Modification de zone sur un device + selects Material | La zone d'un device était figée après création (champ caché dans le formulaire de renommage inline). Le champ `zoneId` de ce formulaire devient un vrai `<select>` (zone courante pré-sélectionnée, "— Aucune zone —" pour retirer l'affectation) — le contrôleur (`DeviceAdminController.rename()`) acceptait déjà `zoneId` en paramètre, seule l'UI manquait ; ajout au passage de la même vérification d'existence de zone que sur la création. Par ailleurs, le sélecteur d'état à déclencher du Mode test (`simulated-device-table.html`) était un `<select>` nu, sans l'habillage Material (`div.field.label.border small round suffix`) déjà utilisé partout ailleurs — corrigé pour cohérence visuelle. **Non vérifié dans le navigateur** (l'utilisateur teste lui-même) — compilation et tests unitaires passent. | — |
| 2026-07-23 | Cohérence Zone/Device : suppression de `deviceIds`, violation de zone recalculée | Corrige le bug du 2026-07-22 (`Zone.deviceIds` jamais synchronisé) et ajoute un état `violation` au niveau zone, en appliquant le même principe aux deux besoins (voir ADR 0017) : `Zone` ne maintient plus aucune collection de devices — `deviceIds`, `withDeviceAdded`/`withDeviceRemoved` supprimés de `Zone`/`ZoneDocument`/`ZoneMapper`/`ZoneResponse`/`ZoneWebMapper` (`CommonIdMapper.uuidSetToStringList`/`stringListToUuidSet`, devenues mortes, supprimées aussi), `DeleteZoneService` interroge désormais `DeviceRepository.findByZoneId` en direct pour `ZoneNotEmptyException`. `Zone.violation` reste persisté mais n'est plus jamais écrit par petites touches : nouveau use-case `RecomputeZoneViolationUseCase`/`RecomputeZoneViolationService` (package `zone`) recalcule entièrement l'état à chaque appel (`findByZoneId` + `filter(Device::isEnabled).anyMatch(Device::isViolation)`, même filtre que `AlarmSystemService.arm()`), appelé depuis `HandleDeviceEventService` (après sauvegarde du device), `UpdateDeviceService` (ancienne **et** nouvelle zone si `zoneId` change) et `DeleteDeviceService` (zone de rattachement avant suppression) — `RegisterDeviceService` n'a pas besoin d'y toucher (`violation=false` à la création). Côté BFF : `ZoneResponse` expose `violation` au lieu de `deviceIds`, `ZoneAdminController` dérive désormais le nombre de devices par zone de `DeviceResponse.zoneId()` (nouveau modèle de présentation `ZoneRow`), `zone-table.html` gagne un badge violation (même style que Devices). **Vérifié** : compilation + tests unitaires des 3 modules touchés (`cerbere-shared-kernel`, `cerbere-core`, `cerbere-bff`) passent — pas de vérification navigateur (l'utilisateur teste lui-même). | [ADR 0017](docs/adr/0017-coherence-zone-device-recalcul-a-la-demande.md) |
| 2026-07-23 | Réévaluation du déclenchement à la réactivation d'un device | Bug repéré par l'utilisateur : un device désactivé reste supervisé (`HandleDeviceEventService` met à jour `Device.violation` même si `enabled=false`, sans jamais déclencher l'alarme tant qu'il est inactif — comportement voulu). Mais si ce device est réactivé pendant que le système est déjà armé, rien ne réévaluait le déclenchement — `AlarmSystemService.arm()` ne fait ce check qu'à l'instant de l'armement, filtré sur les devices *alors* actifs. Un premier essai a fait republier l'état courant depuis `cerbere-devices-mock` à chaque `device.updated` reçu (`UpdateSimulatedDeviceService`), rejeté après explication à l'utilisateur : ça mélange le topic identité/métadonnée (`cerbere.device.state`) et le topic capteur physique (`cerbere.device.events.raw`), et fait qu'un simple renommage ou changement de zone peut re-déclencher l'alarme (le device réémet son dernier état connu, réévalué comme un vrai événement capteur) — changement reverté (`git checkout`). Corrigé côté `cerbere-core` à la source (implémentation initiale via `ReevaluateAlarmTriggerUseCase`, recatégorisée juste après — voir ligne suivante). **Vérifié** : compilation + tests unitaires de `cerbere-core` et `cerbere-devices-mock` passent — pas de vérification navigateur. | — |
| 2026-07-23 | Nouvelle règle : package `application.service` pour les collaborateurs internes | Retour utilisateur sur les deux ajouts précédents (`RecomputeZoneViolationUseCase`, `ReevaluateAlarmTriggerUseCase`) : ce ne sont pas de vrais use-cases (aucun adapter ne les appelle, seulement d'autres classes `application`), donc les exposer comme `port/in` brouille la définition de "driving port" du projet (voir `docs/best-practices/clean-architecture.md`). Règle fixée par l'utilisateur : ce qui n'est pas un port d'entrée ne va pas dans `application.usecase` mais dans `application.service`. Appliqué : les deux interfaces `port/in` supprimées ; `RecomputeZoneViolationService` déménagé vers `application.service` (classe concrète, sans interface) ; la logique de réévaluation extraite de `AlarmSystemService` vers une nouvelle classe dédiée `application.service.AlarmTriggerReevaluationService` (partagée par `AlarmSystemService.arm()` et `UpdateDeviceService`, qui en dépendent désormais par leur type concret plutôt que par une interface — aucune frontière de hexagone n'est franchie entre deux classes `application`). Nouvelle config Spring dédiée `ApplicationServiceConfig`, distincte de `UseCaseConfig`. Documenté dans [ADR 0018](docs/adr/0018-package-application-service-collaborateurs-internes.md) + mise à jour de `coding-standards.md` et `clean-architecture.md`. **Vérifié** : compilation + tests unitaires de `cerbere-core` passent. | [ADR 0018](docs/adr/0018-package-application-service-collaborateurs-internes.md) |
| 2026-07-23 | Module `cerbere-history` implémenté | Passage de scaffold vide à module complet, clean architecture alignée sur `cerbere-core`. Consomme les 3 topics documentés (`cerbere.device.events.raw`, `cerbere.alarm.state-changed`, `cerbere.alarm.alerts`, pas `cerbere.device.state`) via 3 `@KafkaListener` résilients (try/catch + log + swallow, pattern le plus défensif du repo — cerbere-history est la seule couche de durabilité, voir ADR 0006/0014). Domaine : 3 records typés (`DeviceEventRecord`/`AlarmStateChangeRecord`/`AlertRecord`, pas de blob générique) + `AlarmMode`/`AlertSeverity` (copies locales, jamais d'import du domaine `cerbere-core` — même principe que le database-per-service de l'ADR 0003) ; use-cases regroupés par paire record/list (`DeviceEventHistoryService`, `AlarmStateChangeHistoryService`, `AlertHistoryService`, même pattern que `AlarmSystemService`) ; aucun `application.service` nécessaire (pas d'invariant inter-agrégat à maintenir, contrairement à Zone/Device). Persistance Mongo via `MongoTemplate`+`Criteria` (pas de `MongoRepository` dérivé) : le filtrage combine jusqu'à 5 critères tous optionnels, impossible à exprimer proprement en méthode de requête dérivée par nom ; id Mongo = `eventId` pour une idempotence gratuite sur rejeu Kafka. Première pagination du projet (`Page<T>`/`Pageable` Spring en façade REST, nouveau couple `PageRequest`/`PageResult<T>` côté domaine pour ne pas faire fuiter de type framework — voir coding-standards.md). DTOs REST (`DeviceEventHistoryResponse`/`AlarmStateChangeHistoryResponse`/`AlertHistoryResponse`) ajoutés à `cerbere-shared-kernel`. Pas d'écran BFF, pas de TTL Mongo, pas de sécurité Keycloak dans ce tour (voir dette technique). **Vérifié** : compilation + tests unitaires (services + factories event→domaine) de `cerbere-history` passent, pas de régression sur `cerbere-shared-kernel`/`cerbere-core`/`cerbere-bff` — pas de vérification bout-en-bout navigateur/Kafka réel (l'utilisateur teste lui-même). | — |
| 2026-07-24 | Écran BFF : tableau de bord Historique | Nouvel écran `/history` consultant les 3 endpoints de `cerbere-history`. `HistoryCoreClient` (nouveau `historyRestClient`, `cerbere.history.base-url=http://localhost:8084`) + `HistoryPage<T>` (record local reflétant les seuls champs utiles du `Page` Spring Data renvoyé par `cerbere-history`, `@JsonIgnoreProperties(ignoreUnknown = true)` pour tolérer `pageable`/`sort`/`first`/`last` sans les porter côté BFF). `HistoryDashboardController` résout `deviceId`/`zoneId` en libellé/nom (comme les écrans Devices/Zones, jamais d'UUID brut affiché ; fallback "Device supprimé"/"Zone supprimée") et formate `occurredAt` côté Java (`DateTimeFormatter`, pas de dépendance à `#temporals` de Thymeleaf pour éviter un risque de format non supporté sur un `Instant`) ; 3 sections indépendamment paginées (Alertes, Changements d'état, Événements de device), chacune avec ses boutons précédent/suivant en htmx (`hx-target` sur l'id de la table, `hx-swap="outerHTML"`, même convention que les autres écrans). Entrée de menu "Historique" ajoutée à `fragments/nav.html`. **Vérifié bout-en-bout dans le navigateur** (les 4 services + Mongo/Kafka déjà démarrés par l'utilisateur) : les 3 sections affichent des données réelles (libellés/zones résolus, badges de sévérité colorés, payload des événements device affiché), pagination testée (changement de page via htmx, toast de confirmation), aucune erreur console. | — |
| 2026-07-24 | Module `cerbere-devices-bridge` + containerisation Docker Compose + doc NUC | Choix hardware (Zigbee2MQTT, Aqara MCCGQ11LM/RTCGQ11LM, sirène via relais Zigbee générique plutôt que le Heiman HS2WD-E) suivi de trois livrables en parallèle. **`cerbere-devices-bridge`** (nouveau module, clone architectural de `cerbere-devices-mock` sans couche REST/webmvc) : corrélation `friendly_name` Zigbee2MQTT = UUID Cerbère (même principe qu'ADR 0004/0016) ; miroir local `BridgedDevice` alimenté par `cerbere.device.state` ; listener MQTT (Eclipse Paho `org.eclipse.paho.mqttv5.client`, API vérifiée via recherche avant écriture) abonné à `zigbee2mqtt/+`, publie sur `cerbere.device.events.raw` avec le même `eventType`/`payload` que le mock (zéro changement requis côté `cerbere-core`/`cerbere-history`) ; `CommandSirenService` consomme `cerbere.alarm.state-changed` (3ᵉ consommateur indépendant du topic) et commande les sirènes Zigbee (`{"state":"ON"|"OFF"}`) sans nouveau topic ; `CommandSirenUseCase` conservé en véritable `port/in` (ADR 0018 respectée : un adapter, `AlarmStateChangedKafkaConsumer`, l'appelle directement). Documenté dans [ADR 0019](docs/adr/0019-module-devices-bridge-mqtt-zigbee2mqtt.md) + [docs/architecture/mqtt-zigbee-contracts.md](docs/architecture/mqtt-zigbee-contracts.md) (payloads réels par device, mapping vers les conventions Cerbère existantes). **Containerisation** : bloc `<image>` explicite ajouté aux 5 poms concernés (`spring-boot-maven-plugin`, jamais configuré avant — ADR 0008 actée mais jamais appliquée) ; `deployment/docker-compose.yaml` étendu avec `cerbere-core`/`cerbere-bff`/`cerbere-history` toujours démarrés, `cerbere-devices-mock` sous profil `mock`, `cerbere-devices-bridge`+`mosquitto`+`zigbee2mqtt` sous profil `hardware` (mutuellement exclusifs), variables d'environnement pour le réseau inter-conteneurs (`SPRING_KAFKA_BOOTSTRAPSERVERS`, `CERBERE_CORE_BASEURL`, etc. — conversion vérifiée : Spring Boot retire les tirets et remplace les points par des underscores, ne les remplace pas par un underscore). Nouveau [docs/deployment/nuc-deployment.md](docs/deployment/nuc-deployment.md) : séquence de build/déploiement, point d'attention explicite Debian (passthrough USB natif) vs Windows (WSL2+usbipd-win ou Zigbee2MQTT natif hors conteneur) pour le dongle Zigbee, rappel sécurité (permitAll/MQTT anonyme, ne pas exposer sur internet). **Vérifié** : `cerbere-devices-bridge` compile et ses tests unitaires passent (`ReportDeviceStateServiceTest`, `CommandSirenServiceTest`, use-cases centraux, même principe que le reste du repo) — pas de vérification avec du matériel Zigbee réel ni en conteneur (l'utilisateur teste lui-même). | [ADR 0019](docs/adr/0019-module-devices-bridge-mqtt-zigbee2mqtt.md) |

## Décisions d'architecture actives

- [ADR 0001](docs/adr/0001-clean-architecture-par-module.md) — Clean architecture par module (Accepted)
- [ADR 0002](docs/adr/0002-enveloppe-evenement-et-topics-kafka.md) — Enveloppe d'événement et topics Kafka (Accepted)
- [ADR 0003](docs/adr/0003-mongodb-database-per-service.md) — MongoDB database-per-service (Accepted)
- [ADR 0004](docs/adr/0004-module-devices-mock-et-separation-du-registre-device.md) — Module devices-mock séparé (Accepted)
- [ADR 0005](docs/adr/0005-pas-de-librairie-partagee-evenements.md) — Pas de lib partagée pour les événements (Accepted)
- [ADR 0006](docs/adr/0006-notification-copie-locale-des-destinataires.md) — Copie locale des destinataires notification (Accepted)
- [ADR 0007](docs/adr/0007-authentification-keycloak-phase-2.md) — Authentification Keycloak (**Proposed**, phase 2)
- [ADR 0008](docs/adr/0008-strategie-images-cloud-native-buildpacks.md) — Images via Cloud Native Buildpacks (Accepted)
- [ADR 0009](docs/adr/0009-pom-agregateur-racine-optionnel.md) — Pom agrégateur racine sans héritage (Accepted)
- [ADR 0010](docs/adr/0010-module-contract-partage-autorise.md) — Module contract partagé autorisé (Accepted, supersede ADR 0005)
- [ADR 0011](docs/adr/0011-publication-github-packages.md) — Publication GitHub Packages (Accepted)
- [ADR 0012](docs/adr/0012-serializer-kafka-jackson3-maison.md) — Sérialiseur Kafka Jackson 3 écrit à la main (Accepted)
- [ADR 0013](docs/adr/0013-module-cerbere-shared-kernel.md) — Module `cerbere-shared-kernel` (Accepted)
- [ADR 0014](docs/adr/0014-provisionnement-explicite-topics-kafka.md) — Provisionnement explicite des topics Kafka (Accepted)
- [ADR 0015](docs/adr/0015-stack-front-interne-thymeleaf-htmx-beercss.md) — Stack front interne Thymeleaf/htmx/Beer CSS (Accepted)
- [ADR 0016](docs/adr/0016-inversion-dependance-mock-vers-core.md) — Inversion de dépendance mock/core, synchronisation événementielle (Accepted)
- [ADR 0017](docs/adr/0017-coherence-zone-device-recalcul-a-la-demande.md) — Cohérence Zone/Device : recalcul à la demande, pas de miroir persistant (Accepted)
- [ADR 0018](docs/adr/0018-package-application-service-collaborateurs-internes.md) — Package `application.service` pour les collaborateurs internes sans port d'entrée (Accepted)
- [ADR 0019](docs/adr/0019-module-devices-bridge-mqtt-zigbee2mqtt.md) — Module `cerbere-devices-bridge` : supervision du matériel physique via MQTT/Zigbee2MQTT (Accepted)

## Dette technique connue / TODO

- Keycloak non branché : tous les services actuellement en sécurité `permitAll` avec `TODO Keycloak` explicite — ne pas exposer sur internet en l'état. Idem pour `mosquitto.conf` (connexions MQTT anonymes, LAN uniquement).
- `cerbere-notification`, `api-gateway` restent des scaffolds vides — non containerisés (voir `deployment/docker-compose.yaml`), prochaine itération.
- **Résolu le 2026-07-23** (voir ADR 0017) : `Zone.deviceIds` (jamais synchronisé, bug du 2026-07-22) a été supprimé plutôt que corrigé en écriture — `DeleteZoneService` interroge désormais `DeviceRepository.findByZoneId` en direct, et le nombre de devices par zone se dérive côté BFF de `DeviceResponse.zoneId()`.
- **Résolu le 2026-07-24** : `deployment/docker-compose.yaml` contient désormais `cerbere-core`/`cerbere-bff`/`cerbere-history` (toujours démarrés), `cerbere-devices-mock` (profil `mock`) et `cerbere-devices-bridge`+`mosquitto`+`zigbee2mqtt` (profil `hardware`) — le conteneur SMTP factice (maildev) reste à ajouter avec `cerbere-notification`.
- Pas de tests d'intégration Kafka/MQTT avec un vrai broker (embedded/Testcontainers) pour l'instant — tests unitaires sur les use-cases uniquement (la publication réelle a été vérifiée manuellement pour Kafka, pas encore automatisée ; le bridge MQTT n'a pas été testé avec du matériel Zigbee réel ni un broker Mosquitto conteneurisé).
- `spring-kafka` 3.3.8 n'a pas de sérialiseur JSON natif Jackson 3 : contournement centralisé dans `fr.cerbere.shared.kafka.JacksonEventSerializer` (ADR 0012/0013), réutilisable tel quel par les futurs producteurs.
- Pas de pom agrégateur racine réel (ADR 0009 le laissait optionnel) : builder `cerbere-shared-kernel` avant tout module qui en dépend reste une étape manuelle (`mvnw install`) tant qu'il n'est pas créé.
- `cerbere-core`, `cerbere-history` ont supprimé la dépendance `spring-boot-docker-compose` (et leur `compose.yaml`) : à vérifier/nettoyer aussi sur les scaffolds restants (`api-gateway`, `cerbere-bff`, `cerbere-notification`) quand ils seront implémentés, pour éviter le même conflit avec `deployment/docker-compose.yaml`. `cerbere-bff` n'a jamais eu cette dépendance (scaffold créé directement sans starter Initializr), `cerbere-devices-bridge` non plus (scaffolder manuellement).
- `cerbere-history` ne consomme pas `cerbere.device.state` (hors périmètre documenté) : les réponses REST exposent `deviceId`/`zoneId` bruts, la résolution en libellé reviendra à un futur écran BFF. Pas de TTL/rétention Mongo (conservation indéfinie) ni d'écran BFF pour l'historique — prochaines itérations si besoin.
- **Résolu le 2026-07-22 puis 2026-07-24** (voir ADR 0016 et [ADR 0019](docs/adr/0019-module-devices-bridge-mqtt-zigbee2mqtt.md)) : la demande de transparence totale sur l'id device (pas de copier-coller manuel d'UUID) est satisfaite pour la simulation **et** pour le matériel réel — le BFF génère l'id à la création dans `cerbere-core`, propagé automatiquement à `cerbere-devices-mock`/`cerbere-devices-bridge` par événement Kafka. Reste une étape manuelle pour le matériel réel : renommer le `friendly_name` Zigbee2MQTT pour qu'il corresponde à cet UUID à l'appairage (voir docs/architecture/mqtt-zigbee-contracts.md).
- Propagation Kafka asynchrone entre la création d'un device dans `cerbere-core` et son apparition dans le mode test du BFF (voir ADR 0016) : pas de souci observé à l'échelle actuelle (délai de l'ordre de la milliseconde en local), à surveiller si le volume augmente.
- Aucun NUC n'a encore été provisionné : `docs/deployment/nuc-deployment.md` décrit une procédure prévue, pas encore vérifiée en conditions réelles (passthrough USB du dongle Zigbee notamment).

## Règles de mise à jour pour l'IA

- À chaque itération : ajouter une ligne au **Journal des itérations** ci-dessus (date, ce qui a été livré, liens PRD/ADR).
- Toute décision d'architecture structurante (nouveau choix technique, nouveau pattern, changement de convention) → créer un nouvel ADR dans `docs/adr/`, le référencer dans ce fichier.
- Mettre à jour le tableau **Structure du repo** dès qu'un module change de statut (scaffold → implémenté).
- Mettre à jour `docs/architecture/vue-ensemble.md` si le schéma de communication entre services change.
- Ne jamais laisser ce fichier décrire du code qui n'existe pas encore comme s'il existait — utiliser "À venir"/"Scaffold vide" explicitement.
